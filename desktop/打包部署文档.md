# Tauri 桌面应用打包部署指南

本文档介绍如何将 Sentra Frontend 桌面应用打包成可分发的安装程序，让其他用户可以安装使用。

## 目录
- [准备工作](#准备工作)
- [打包流程](#打包流程)
- [平台特定说明](#平台特定说明)
- [分发方法](#分发方法)
- [常见问题](#常见问题)

---

## 准备工作

### 1. 系统要求

#### Windows 打包
- **操作系统**: Windows 10 或更高版本
- **必需工具**:
  - [Rust](https://www.rust-lang.org/tools/install) (1.70+)
  - [Node.js](https://nodejs.org/) (18+)
  - [WebView2](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) (Windows 10/11 通常已预装)
  - [Visual Studio C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)

#### macOS 打包
- **操作系统**: macOS 10.15 (Catalina) 或更高版本
- **必需工具**:
  - [Rust](https://www.rust-lang.org/tools/install)
  - [Node.js](https://nodejs.org/) (18+)
  - [Xcode](https://developer.apple.com/xcode/) 及其命令行工具

#### Linux 打包
- **操作系统**: Ubuntu 20.04+ / Debian 11+ / Fedora / etc.
- **必需工具**:
  - [Rust](https://www.rust-lang.org/tools/install)
  - [Node.js](https://nodejs.org/) (18+)
  - 系统包管理器及相关开发库

### 2. 验证环境

在打包前，请确认开发环境正常：

```bash
cd desktop-dev
npm run tauri dev
```

如果应用能正常启动，说明环境配置正确。

---

## 打包流程

### 步骤 1: 清理之前的构建

```bash
cd desktop-dev
npm run tauri build -- --clean
```

### 步骤 2: 执行打包命令

```bash
npm run tauri build
```

这个命令会：
1. 构建前端项目（Next.js 生产版本）
2. 编译 Rust 后端代码
3. 打包成平台特定的安装程序
4. 生成所有必要的图标和资源

**预计耗时**: 首次打包 5-15 分钟（取决于电脑性能）

### 步骤 3: 查找生成的安装包

打包完成后，安装包位于：

```
desktop-dev/src-tauri/target/release/bundle/
```

---

## 平台特定说明

### Windows 平台

#### 生成的文件
```
src-tauri/target/release/bundle/
├── msi/                    # MSI 安装程序
│   └── Sentra_Frontend_1.0.0_x64_en-US.msi
├── nsis/                   # NSIS 安装程序（推荐）
│   ├── Sentra_Frontend_1.0.0_x64-setup.exe  # 安装程序
│   └── Sentra_Frontend_1.0.0_x64-setup.exe.zip  # 压缩包
└── wix/                    # WiX 工具包
```

#### 推荐分发格式
- **NSIS 安装程序** (`Sentra_Frontend_1.0.0_x64-setup.exe`)
  - 体积小
  - 支持自定义安装选项
  - 兼容性好

#### 用户安装步骤
1. 下载 `Sentra_Frontend_1.0.0_x64-setup.exe`
2. 双击运行安装程序
3. 按照安装向导完成安装
4. 从开始菜单启动应用

#### 配置签名（可选但推荐）

为了防止 Windows SmartScreen 警告，建议对应用进行签名：

1. **获取代码签名证书**
   - 从 [DigiCert](https://www.digicert.com/)、[Sectigo](https://sectigo.com/) 等机构购买
   - 或使用 [SignPath](https://signpath.io/) 的免费证书

2. **配置签名**
   编辑 `src-tauri/tauri.conf.json`:
   ```json
   {
     "tauri": {
       "bundle": {
         "windows": {
           "wix": {
             "language": "zh-CN"
           },
           "webviewInstallMode": {
             "type": "embedBootstrapper"
           },
           "certificateThumbprint": "你的证书指纹",
           "timestampUrl": "http://timestamp.digicert.com"
         }
       }
     }
   }
   ```

### macOS 平台

#### 生成的文件
```
src-tauri/target/release/bundle/
├── dmg/                    # DMG 镜像文件
│   └── Sentra Frontend_1.0.0_x64.dmg
├── app/                    # 应用程序包
│   └── Sentra Frontend.app
└── macos/                  # 其他格式
```

#### 推荐分发格式
- **DMG 磁盘镜像** (`Sentra Frontend_1.0.0_x64.dmg`)

#### 用户安装步骤
1. 下载 `.dmg` 文件
2. 双击打开磁盘镜像
3. 将应用拖拽到"应用程序"文件夹
4. 从 Launchpad 或应用程序文件夹启动

#### 代码签名（必须）

macOS 要求应用必须签名才能分发：

```bash
# 设置开发者身份
export APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (TEAM_ID)"

# 打包并签名
npm run tauri build
```

公证（Notarization）流程（macOS 10.15+ 必需）：

1. 上传应用到 Apple
2. 等待公证完成
3. 下载已公证的应用

### Linux 平台

#### 生成的文件
```
src-tauri/target/release/bundle/
├── deb/                    # Debian 包
│   └── sentra-frontend_1.0.0_amd64.deb
├── appimage/               # AppImage (通用)
│   └── sentra-frontend_1.0.0_amd64.AppImage
└── rpm/                    # Fedora/Red Hat 包
```

#### 推荐分发格式
- **AppImage** - 通用格式，适用于所有 Linux 发行版
- **Deb 包** - 适用于 Debian/Ubuntu 系
- **RPM 包** - 适用于 Fedora/RedHat 系

#### 用户安装步骤

**AppImage:**
```bash
chmod +x sentra-frontend_1.0.0_amd64.AppImage
./sentra-frontend_1.0.0_amd64.AppImage
```

**Deb 包:**
```bash
sudo dpkg -i sentra-frontend_1.0.0_amd64.deb
sudo apt-get install -f  # 修复依赖
```

**RPM 包:**
```bash
sudo rpm -i sentra-frontend-1.0.0.x86_64.rpm
```

---

## 分发方法

### 1. 直接文件分发

**适用场景**: 小范围分发、内部测试

将生成的安装包上传到：
- 公司内部文件服务器
- Google Drive / OneDrive / Dropbox
- QQ/微信 群文件

**优点**: 简单快速
**缺点**: 无法自动更新，难以管理版本

### 2. GitHub Releases

**适用场景**: 开源项目、面向开发者的工具

1. 在 GitHub 创建 Release:
   ```bash
   # 使用 Git CLI
   gh release create v1.0.0 \
     "src-tauri/target/release/bundle/nsis/Sentra_Frontend_1.0.0_x64-setup.exe" \
     "src-tauri/target/release/bundle/dmg/Sentra Frontend_1.0.0_x64.dmg" \
     --title "Sentra Frontend v1.0.0" \
     --notes "第一个正式发布版本"
   ```

2. 用户可以从 Releases 页面下载对应平台的安装包

**优点**: 版本管理清晰，支持多平台
**缺点**: 需要公开仓库（私有仓库也可以）

### 3. 自建更新服务器

**适用场景**: 面向普通用户的产品

#### 搭建步骤

1. **创建更新配置文件**

创建 `src-tauri/tauri.conf.json` 中的 updater 配置:

```json
{
  "tauri": {
    "bundle": {
      "publisher": "Your Company",
      "website": "https://your-website.com",
      "updater": {
        "active": true,
        "endpoints": [
          "https://update-server.com/update/{{target}}/{{current_version}}"
        ],
        "dialog": true,
        "pubkey": "YOUR_PUBLIC_KEY_HERE"
      }
    }
  }
}
```

2. **生成签名密钥对**

```bash
# 生成私钥（仅打包时使用）
npm run tauri signer generate

# 将生成的公钥配置到 tauri.conf.json
# 私钥保管好，用于签名更新包
```

3. **创建更新服务器 API**

更新服务器需要返回 JSON 格式:

```json
{
  "url": "https://cdn.example.com/downloads/Sentra_Frontend_1.0.1_x64-setup.exe",
  "version": "1.0.1",
  "notes": "Bug fixes and performance improvements",
  "pub_date": "2025-01-29T00:00:00Z",
  "signature": "SIGNATURE_HERE"
}
```

4. **签名并发布更新**

```bash
# 签名更新包
npm run tauri signer sign "path/to/installer.exe" "path/to/private.key"

# 上传到 CDN
# 更新服务器 API 指向新版本
```

#### 示例更新服务器（Node.js）

```javascript
// server.js
const express = require('express');
const app = express();

const versions = {
  'windows-x86_64': {
    version: '1.0.1',
    url: 'https://cdn.example.com/Sentra_1.0.1_setup.exe',
    notes: '修复了若干 bug',
    pub_date: new Date().toISOString()
  },
  'darwin-x86_64': {
    version: '1.0.1',
    url: 'https://cdn.example.com/Sentra_1.0.1.dmg',
    notes: '修复了若干 bug',
    pub_date: new Date().toISOString()
  }
};

app.get('/update/:target/:current_version', (req, res) => {
  const target = req.params.target; // windows-x86_64, darwin-x86_64
  const currentVersion = req.params.current_version;

  const latest = versions[target];

  if (!latest) {
    return res.status(404).json({ error: 'Platform not supported' });
  }

  // 比较版本，如果当前版本不是最新则返回更新信息
  if (currentVersion !== latest.version) {
    res.json(latest);
  } else {
    res.status(204).send(); // 无更新
  }
});

app.listen(3000, () => {
  console.log('Update server running on port 3000');
});
```

### 4. 第三方分发平台

- **微软商店** - 需要注册开发者账号 ($19/年)
- **Mac App Store** - 需要 Apple Developer Program ($99/年)
- **Snap Store** (Linux) - 免费

---

## 打包优化建议

### 1. 减小安装包体积

#### 启用压缩

编辑 `src-tauri/tauri.conf.json`:

```json
{
  "tauri": {
    "bundle": {
      "windows": {
        "nsis": {
          "displayLanguageSelector": false,
          "compressionLevel": "ultra"
        }
      }
    }
  }
}
```

#### 优化前端构建

在 `next.config.js`:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // 减小 Next.js 输出体积
  compress: true,
  productionBrowserSourceMaps: false
}
```

### 2. 自动化打包脚本

创建 `scripts/build-all.sh`:

```bash
#!/bin/bash

echo "开始打包 Sentra Frontend..."

# 清理旧文件
rm -rf src-tauri/target/release/bundle

# 打包
npm run tauri build

# 复制安装包到 dist 目录
mkdir -p dist
cp src-tauri/target/release/bundle/nsis/*.exe dist/
cp src-tauri/target/release/bundle/dmg/*.dmg dist/
cp src-tauri/target/release/bundle/appimage/*.AppImage dist/

# 生成校验和
cd dist
sha256sum * > checksums.txt

echo "打包完成！安装包位于 dist/ 目录"
```

使用:
```bash
chmod +x scripts/build-all.sh
./scripts/build-all.sh
```

### 3. CI/CD 自动化

#### GitHub Actions 示例

创建 `.github/workflows/build.yml`:

```yaml
name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri app
        run: npm run tauri build

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.os }}-bundle
          path: src-tauri/target/release/bundle/

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v3

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            **/*.exe
            **/*.dmg
            **/*.AppImage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---

## 版本管理

### 更新版本号

1. **修改 package.json**
   ```json
   {
     "version": "1.0.1"
   }
   ```

2. **修改 tauri.conf.json**
   ```json
   {
     "package": {
       "version": "1.0.1"
     }
   }
   ```

3. **重新打包**
   ```bash
   npm run tauri build
   ```

### 版本号规范

推荐使用 [语义化版本](https://semver.org/lang/zh-CN/):
- **主版本号 (Major)**: 不兼容的 API 修改 (如 1.0.0 → 2.0.0)
- **次版本号 (Minor)**: 向下兼容的功能性新增 (如 1.0.0 → 1.1.0)
- **修订号 (Patch)**: 向下兼容的问题修正 (如 1.0.0 → 1.0.1)

---

## 常见问题

### Q1: Windows 安装包被 SmartScreen 阻止？

**原因**: 应用未经签名或信誉不足

**解决方案**:
1. 对应用进行代码签名（推荐）
2. 提醒用户"更多信息"→"仍要运行"
3. 累积下载量和用户反馈后，SmartScreen 会自动信任

### Q2: macOS 提示"已损坏，无法打开"？

**原因**: 未签名或未公证

**解决方案**:
1. 确保使用有效的开发者证书签名
2. 进行公证（Notarization）
3. 用户可以右键点击 →"打开"→"打开"绕过限制（不推荐）

### Q3: Linux AppImage 无法运行？

**检查**:
```bash
chmod +x sentra-frontend_*.AppImage
./sentra-frontend_*.AppImage --appimage-extract-and-run
```

**常见原因**:
- 缺少依赖库（检查 `ldd` 输出）
- FUSE 权限问题

### Q4: 打包失败，提示缺少依赖？

**Windows**:
```bash
# 安装 WebView2
# https://developer.microsoft.com/en-us/microsoft-edge/webview2/
```

**macOS**:
```bash
# 安装 Xcode 命令行工具
xcode-select --install
```

**Linux (Ubuntu/Debian)**:
```bash
sudo apt update
sudo apt install libwebkit2gtk-4.1-dev \
  build-essential \
  curl \
  wget \
  file \
  libssl-dev \
  libayatana-appindicator3-dev \
  librsvg2-dev
```

### Q5: 打包后的应用体积太大？

**优化方法**:
1. 启用最大压缩
2. 检查是否有未使用的前端资源
3. 使用 `tauri info` 查看各部分体积
4. 考虑拆分多平台安装包

### Q6: 如何测试安装包？

**Windows**:
```bash
# 在虚拟机中测试
# 或使用 Sandboxie 隔离测试
```

**macOS**:
```bash
# 在干净的虚拟机中测试
# 检查 Gatekeeper 设置
```

**通用测试清单**:
- [ ] 安装过程是否顺利
- [ ] 应用能否正常启动
- [ ] 主要功能是否正常
- [ ] 卸载是否干净
- [ ] 更新流程是否正常

---

## 总结

快速打包命令:

```bash
# 1. 确保环境正常
npm run tauri dev

# 2. 执行打包
npm run tauri build

# 3. 找到安装包
# Windows: src-tauri/target/release/bundle/nsis/*.exe
# macOS: src-tauri/target/release/bundle/dmg/*.dmg
# Linux: src-tauri/target/release/bundle/appimage/*.AppImage

# 4. 分发给用户
# - 直接发送文件
# - 上传到云盘
# - 发布到 GitHub Releases
# - 部署更新服务器
```

祝你打包顺利！🚀

---

**相关链接**:
- [Tauri 官方文档](https://tauri.app/v1/guides/)
- [Next.js 部署文档](https://nextjs.org/docs/deployment)
- [语义化版本规范](https://semver.org/lang/zh-CN/)
